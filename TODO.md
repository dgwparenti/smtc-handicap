# TODO

- [x] Archive plans to build the models and the UI
- [ ] Of the existing TOP models, find the one that met the two exit criteria of the Ralph Wiggum loop (avg tightness of top 5 riders < 1s and > 70% of races the Bayesian model had better tightness than the handicap committee) and keep only that model
- [ ] Build a script that will incrementally ingest practice time sheets and refit the model based on the existing formulation including the existing and new data
- [ ] Build a script that for race time sheets will estimate the Bayesian handicap and then calculate the tightness for the top 3, 6, 10 riders of the race
- [ ] Build a standard Ralph Wiggum loop script to improve the model formulation to meet the two exit criteria
